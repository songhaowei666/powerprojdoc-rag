这是关于生成readme的提示词



系统架构图


主要步骤
1 数据导入
代码在 src/pdf_mineru.py   输出是md文件


通过mineru得到json格式的解析结果

    file_name = '【财报】中芯国际：中芯国际2024年年度报告.pdf'
    task_id = get_task_id(file_name)
    print('task_id:',task_id)
    get_result(task_id)

    示例文档在  data/projdoc_data/debug_data/MinerU_【财报】中芯国际：中芯国际2024年年度报告__20260520083937.json

把解析结果转换成按页索引的md文档
 merger = MinerUReportMerger()
    reports = merger.process_reports(
        reports_paths=[json_path],
        output_dir=output_dir,
        subset_csv=subset_csv,
    )
示例文档在  data/projdoc_data/debug_data/02_merged_reports/【财报】中芯国际：中芯国际2024年年度报告.json
2 文本分块

示例代码
    """
    本地调试入口：对 merged_reports 目录下的 JSON 报告进行文本分块测试。
    """
    root = Path(__file__).resolve().parent.parent
    text_splitter = TextSplitter()

    # 默认路径与 PipelineConfig 保持一致
    merged_reports_path = root / "data" / "projdoc_data" / "debug_data" / "02_merged_reports"
    documents_dir = root / "data" / "projdoc_data" / "databases" / "chunked_reports"
    serialized_tables_dir = None

    text_splitter.split_all_reports(
        all_report_dir=merged_reports_path,
        output_dir=documents_dir,
        serialized_tables_dir=serialized_tables_dir,
    )

示例文档在 data/projdoc_data/databases/chunked_reports/【财报】中芯国际：中芯国际2024年年度报告.json
3 信息嵌入


4 向量存储


5 检索前处理
查询重写  改成更适合本系统的（查询多样性，用户不一定能够描述清楚他们的关注点）

查询路由  查询不同的提示词   用户大部分的问题都是类似的，有点长尾效应。对于出现频率比较高的问题。设置提示词模版rag系统，匹配度高的话直接采用，对于低频问题，使用通用的提示词模版

6 索引优化
分子文本块
子文本->父文本

7 检索后处理
重排 （rrf或重排序模型重排）

校正（降低模型的幻觉）


8 系统评估（还未完成）
评估数据集

检索评估
页面精确率（基于数据集）

上下文精确度

响应评估
忠实度（大模型的回答是否基于检索到的上下文）
答案相关性（生成的回答是否回应了用户的查询）

9 最后  用star写 一段，不要太长。大致分为 生成器。检索器。系统评估。 三个模块。放简历找工作用